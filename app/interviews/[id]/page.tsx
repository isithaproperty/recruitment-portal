"use client";

import Link from "next/link";
import { use, useEffect, useMemo, useState } from "react";
import { createClient } from "@/lib/supabase/client";
import { ISITHA_LOGO_DATA_URI } from "@/lib/brand";
import FeedbackActions from "./FeedbackActions";

type Feedback={id:string;round_number:number;interviewer:string|null;interview_date:string|null;technical_rating:number|null;experience_rating:number|null;communication_rating:number|null;team_fit_rating:number|null;overall_rating:number|null;strengths:string|null;weaknesses:string|null;salary_comments:string|null;availability_comments:string|null;general_comments:string|null;outcome:string|null;submitted_at:string};
type InterviewRecord={id:string;interview_status:string;interview_scheduled_at:string|null;interview_location:string|null;interview_meeting_link:string|null;interview_notes:string|null;candidate_applications:{candidate_name:string}|null;client_submissions:{recruitment_clients:{company_name:string}|null;jobs:{title:string}|null}|null};

export default function InterviewPage({params}:{params:Promise<{id:string}>}){
 const{id}=use(params);const supabase=useMemo(()=>createClient(),[]);const[record,setRecord]=useState<InterviewRecord|null>(null);const[feedback,setFeedback]=useState<Feedback[]>([]);const[loading,setLoading]=useState(true);const[message,setMessage]=useState("");const[busy,setBusy]=useState(false);
 useEffect(()=>{void load()},[id]);
 async function load(){setLoading(true);const[r,f]=await Promise.all([supabase.from("client_submission_candidates").select("id,interview_status,interview_scheduled_at,interview_location,interview_meeting_link,interview_notes,candidate_applications(candidate_name),client_submissions(recruitment_clients(company_name),jobs(title))").eq("id",id).single(),supabase.from("client_interview_feedback").select("id,round_number,interviewer,interview_date,technical_rating,experience_rating,communication_rating,team_fit_rating,overall_rating,strengths,weaknesses,salary_comments,availability_comments,general_comments,outcome,submitted_at").eq("submission_candidate_id",id).order("round_number")]);if(r.error){setMessage(r.error.message);setRecord(null)}else setRecord(r.data as unknown as InterviewRecord);setFeedback((f.data||[]) as Feedback[]);if(f.error)setMessage(f.error.message);setLoading(false)}
 async function schedule(form:HTMLFormElement){const fd=new FormData(form);const when=String(fd.get("when")||"");if(!when)return setMessage("Choose the interview date and time.");setBusy(true);const{error}=await supabase.from("client_submission_candidates").update({interview_status:"scheduled",interview_scheduled_at:new Date(when).toISOString(),interview_location:String(fd.get("location")||"")||null,interview_meeting_link:String(fd.get("link")||"")||null,interview_notes:String(fd.get("notes")||"")||null}).eq("id",id);setBusy(false);if(error)return setMessage(error.message);setMessage("Interview scheduled.");await load()}
 async function complete(){setBusy(true);const{error}=await supabase.from("client_submission_candidates").update({interview_status:"completed",interview_completed_at:new Date().toISOString()}).eq("id",id);setBusy(false);if(error)return setMessage(error.message);setMessage("Interview marked completed.");await load()}
 if(loading)return <main className="min-h-screen bg-[#f4f6f8] p-8">Loading interview request…</main>;
 if(!record)return <main className="min-h-screen bg-[#f4f6f8] p-8"><p>{message||"Interview request not found."}</p><Link href="/" className="mt-4 inline-block font-bold underline">Back to dashboard</Link></main>;
 const candidate=record.candidate_applications?.candidate_name||"Candidate";const client=record.client_submissions?.recruitment_clients?.company_name||"Client";const role=record.client_submissions?.jobs?.title||"Role";
 return <main className="min-h-screen bg-[#f4f6f8] text-[#172536]"><div className="mx-auto max-w-3xl px-5 py-7"><header className="flex flex-wrap items-center justify-between gap-4 border-b-2 border-[#c89a4b]/45 pb-5"><img src={ISITHA_LOGO_DATA_URI} alt="Isitha Global" className="w-[190px]"/><Link href="/" className="rounded-lg bg-[#0b2239] px-4 py-2 text-sm font-bold text-white">Back to dashboard</Link></header><section className="py-8"><p className="text-xs font-extrabold uppercase tracking-widest text-[#a88436]">Interview Record</p><h1 className="mt-2 text-3xl font-extrabold">{candidate}</h1><p className="mt-2 text-[#667085]">{client} · {role}</p></section>{message&&<div className="mb-5 rounded-xl border border-[#c89a4b]/40 bg-[#fff9ec] p-4 text-sm font-semibold">{message}</div>}<section className="rounded-2xl border bg-white p-6 shadow-sm"><div className="flex items-center justify-between gap-3"><h2 className="text-xl font-bold">Interview management</h2><span className="rounded-full bg-[#f3ead8] px-3 py-1 text-xs font-bold uppercase">{record.interview_status.replaceAll('_',' ')}</span></div>{record.interview_status==='requested'&&<form className="mt-5 grid gap-3 sm:grid-cols-2" onSubmit={e=>{e.preventDefault();void schedule(e.currentTarget)}}><label className="text-sm font-semibold">Date & time<input name="when" type="datetime-local" required className="mt-1 w-full rounded-lg border px-3 py-2"/></label><label className="text-sm font-semibold">Location / platform<input name="location" placeholder="Office / Teams / Zoom" className="mt-1 w-full rounded-lg border px-3 py-2"/></label><label className="text-sm font-semibold sm:col-span-2">Meeting link<input name="link" placeholder="Optional meeting link" className="mt-1 w-full rounded-lg border px-3 py-2"/></label><label className="text-sm font-semibold sm:col-span-2">Notes<textarea name="notes" placeholder="Optional interview notes" className="mt-1 w-full rounded-lg border px-3 py-2"/></label><button disabled={busy} className="rounded-lg bg-[#0b2239] px-5 py-3 font-bold text-white sm:col-span-2">Schedule interview</button></form>}{(record.interview_status==='scheduled'||record.interview_status==='completed')&&<div className="mt-5 rounded-xl bg-slate-50 p-5"><p><b>Scheduled:</b> {record.interview_scheduled_at?new Date(record.interview_scheduled_at).toLocaleString():'-'}</p>{record.interview_location&&<p className="mt-1">{record.interview_location}</p>}{record.interview_meeting_link&&<p className="mt-1"><a href={record.interview_meeting_link} target="_blank" className="font-bold underline">Open meeting link</a></p>}{record.interview_notes&&<p className="mt-2 text-sm text-[#667085]">{record.interview_notes}</p>}{record.interview_status==='scheduled'&&<button disabled={busy} onClick={complete} className="mt-4 rounded-lg bg-[#c89a4b] px-5 py-3 font-extrabold text-[#0b2239]">Mark interview completed</button>}</div>}</section>{feedback.length>0&&<section className="mt-6 rounded-2xl border-2 border-[#c89a4b]/40 bg-white p-6 shadow-sm"><p className="text-xs font-extrabold uppercase tracking-widest text-[#a88436]">Client Feedback</p><h2 className="mt-1 text-2xl font-extrabold">Interview feedback received</h2><div className="mt-5 space-y-4">{feedback.map(f=><div key={f.id} className="rounded-xl border bg-[#fff9ec] p-5"><div className="flex flex-wrap items-center justify-between gap-3"><p className="font-extrabold">Round {f.round_number}</p><span className="rounded-full bg-white px-3 py-1 text-xs font-bold uppercase">{f.outcome?.replaceAll('_',' ')||'submitted'}</span></div><div className="mt-4 grid gap-3 sm:grid-cols-2"><p><b>Interviewer:</b> {f.interviewer||'-'}</p><p><b>Overall:</b> {f.overall_rating?`${f.overall_rating}/5`:'-'}</p><p><b>Technical:</b> {f.technical_rating?`${f.technical_rating}/5`:'-'}</p><p><b>Experience:</b> {f.experience_rating?`${f.experience_rating}/5`:'-'}</p><p><b>Communication:</b> {f.communication_rating?`${f.communication_rating}/5`:'-'}</p><p><b>Team fit:</b> {f.team_fit_rating?`${f.team_fit_rating}/5`:'-'}</p></div>{f.strengths&&<div className="mt-4"><b>Strengths</b><p className="mt-1 whitespace-pre-wrap text-sm">{f.strengths}</p></div>}{f.weaknesses&&<div className="mt-4"><b>Weaknesses / concerns</b><p className="mt-1 whitespace-pre-wrap text-sm">{f.weaknesses}</p></div>}{f.salary_comments&&<div className="mt-4"><b>Salary comments</b><p className="mt-1 whitespace-pre-wrap text-sm">{f.salary_comments}</p></div>}{f.availability_comments&&<div className="mt-4"><b>Availability</b><p className="mt-1 whitespace-pre-wrap text-sm">{f.availability_comments}</p></div>}{f.general_comments&&<div className="mt-4"><b>General comments</b><p className="mt-1 whitespace-pre-wrap text-sm">{f.general_comments}</p></div>}<FeedbackActions feedback={f} candidate={candidate} client={client} role={role} onDeleted={deletedId=>setFeedback(current=>current.filter(item=>item.id!==deletedId))}/></div>)}</div></section>}</div></main>;
}
